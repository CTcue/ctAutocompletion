

// TODO: Look for a 'local_config.json' to override settings

module.exports = {
    "host" : "localhost",
    "port" : 4080,

    "elasticsearch": {
        "version": "6.2",
        "host"   : "localhost",
        "auth"   : "elastic:changeme"
    }
};
